# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index()
#sGET     /getAllCategories           controllers.ProductController.index()
GET     /createFakeObjects          controllers.ProductController.createObjects()
GET     /getOneProduct/:id         	controllers.ProductController.getOneProduct(id : Int)
GET     /getAllProducts            	controllers.ProductController.getAllProducts()




# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
